import type { Metadata } from "next";

export const metadata: Metadata = { title: "Рабочее пространство — Dream Weaver Studio" };

export default function WorkspaceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
