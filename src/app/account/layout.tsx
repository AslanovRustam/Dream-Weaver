import type { Metadata } from "next";

export const metadata: Metadata = { title: "Аккаунт — GenGO" };

export default function AccountLayout({ children }: { children: React.ReactNode }) {
  return children;
}
